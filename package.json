{
  "name": "pcstats-rates-site",
  "version": "1.0.0",
  "description": "Backend + site: currency rates per server and player nicknames per server for PCStats.lua",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
